{
  "name": "weather-effect-gnome",
  "private": true,
  "version": "1.1.0",
  "description": "Adds a weather effect (snow or rain) to your GNOME wallpaper.",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp dist/extension.js extension.js && cp dist/prefs.js prefs.js && rm -rf dist && ./install.sh",
    "watch": "tsc -p tsconfig.json -w"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
